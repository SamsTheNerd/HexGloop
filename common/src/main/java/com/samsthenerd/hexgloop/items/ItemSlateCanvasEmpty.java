package com.samsthenerd.hexgloop.items;

import net.minecraft.item.Item;

// might need to extend NetworkSynced ? idk probably not
public class ItemSlateCanvasEmpty extends Item{
    public ItemSlateCanvasEmpty(Settings settings) {
        super(settings);
    }
}
